
<table>
    <h3>年間集計表</h3>
</table>
<table>
 <%= form_tag("/days/ytotal",method: "get") do %>
    <select name="year">
    <option value="">選択してください</option>
    <option value="2019">2019</option>
    <option value="2020">2020</option>
    <option value="2021">2021</option>
    <option value="2022">2022</option>
    </select>
  <%= label_tag(:year,"年") %>

  <%= submit_tag("検索") %>
 <% end %>
</table>

<table>
   <tr>
    <td><%= "#{params[:year]}" %>年</td>
    
   </tr>
</table>

<table>

<% @days_total.each do |k,v| %>
   <%= "#{k} = #{v}" %> 
     <% end %>


<%
=begin
%>
 <% @days_total.each do |k,v| %>
   <%= "#{k} = #{v}" %> 
     <% end %>
 <% @days_total.each_slice(2) do |k,v|%> 
       <%= "#{k}" %>
      <%= "#{v}" %>   
  <% @days_total.each do |k,v|%>
  <%= "#{k} = #{v}" %>
  <% end %
  <% @days.each do |day| %>
  <%= day.month %>
  <%= day.name %>
  <%= day.value %></br>
  <% end %>
<%
=end
%>

</table>

<table>
 <%= link_to '一覧に戻る', days_path %>
</table>